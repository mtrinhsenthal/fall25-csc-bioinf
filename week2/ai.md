LLM version: ChatGPT (GPT-5)
Prompt: how can i make my own @test decorator in codon?
Use: to automate our own test runner for codon tests, since unittest.TextTestRunner did not seem to be supported yet.

LLM version: ChatGPT (GPT-5)
Prompt: can you break up my __getitem__ function into smaller ones so that i only ever have 1 or 2 return types, so that it looks like the get_value function i have (copied and pasted my get_value function)
Use: to bypass __getitem__, which had too many return types and was causing Codon to complain