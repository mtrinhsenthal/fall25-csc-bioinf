## Steps
Write the global alignment algorithm, modify it for local alignment, modify the global for affine alignment, and the fitting alignment. Write the timing function in evaluate.sh. I used chatgpt for affine and fitting alignment, then modified them to be correct. I used chatgpt to write the evaluate.sh script as well, which required some modification too.

## Gotchas
- Running affine_alignment.py with MT-human works on my local machine but fails with error 143 on Github actions.
- q2 tests fail, didn't have enough time to figure out why

## Time Spent
10 hours